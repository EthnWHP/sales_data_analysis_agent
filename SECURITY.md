# 安全与数据处理

该项目是本地学习 Demo，没有鉴权、沙箱或多人数据隔离，不应直接暴露为公网服务。模型提示词不是完整的安全边界。

- 不提交真实 `.env`、API 密钥、Redis 凭据、客户数据或原始对话。
- 离线 `demo.py` 不发送网络请求；Agent 会向所配置模型服务发送问题、上下文及工具结果。
- Redis 会保存消息和工具结果；按数据敏感程度配置访问控制、保留期及清理流程。
- 已跟踪文件不受新增忽略规则保护；发现历史凭据泄露应先撤销或轮换，再处理历史副本。
- 不要把安全问题的复现密钥、私人地址或业务数据写进公开 Issue。若仓库提供私密漏洞报告入口，请使用该入口；否则先通过已有私下渠道联系维护者，不在此虚构联系方式。

当前测试覆盖功能正确性，不等价于全面安全审计；未对真实服务商、Redis 部署或生产数据进行安全认证。
